# T03 - Maximum 62-byte N-SDU

- **Phase / environment:** Phase 1 with Phase 2 enabled, deterministic host
  fixture, 1 ms tick.
- **Config:** Data CAN ID `0x650`, FC CAN ID `0x658`, DLC 8, BS 4,
  STmin 5 ms.
- **Input:** payload `00..3D`.
- **Expected:** one FF, eight CF, two CTS, no third FC; one Tx snapshot and one
  62-byte Rx copy.
- **Actual Data trace:** FF at tick 1; CF1-CF8 at ticks
  `2, 7, 12, 17, 22, 27, 32, 37`; PCI bytes `21..28` and final byte `3D`.
- **Actual FC trace:** exactly two frames, both
  `30 04 05 00 00 00 00 00`; no FC3.
- **Actual commits:** offsets `6, 13, 20, 27, 34, 41, 48, 55, 62`.
- **Actual callbacks/payload:** `CopyTxData=1`, Tx final count 1/result `E_OK`;
  `StartOfReception=1`, `CopyRxData=1`, Rx final count 1/result `E_OK`;
  length 62 and payload `00..3D` matched.
- **Actual queue:** READY before final indication and consumed as an identical
  62-byte payload.
- **Result:** **PASS**.
- **Evidence:** `EVIDENCE T03` lines in
  [protocol_trace.log](raw/protocol_trace.log) and
  [routing_trace.log](raw/routing_trace.log).

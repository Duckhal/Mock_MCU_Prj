# T08 - N_Cr restart and timeout cleanup

- **Phase / environment:** Phase 2, deterministic 1 ms host tick.
- **Config:** Data CAN ID `0x650`, FC CAN ID `0x658`, DLC 8,
  `N_Cr=100 ms`.
- **Setup:** receive FF20, confirm local CTS, receive valid CF1, then suppress
  CF2.
- **Expected:** N_Cr starts at CTS confirmation, restarts after CF1, expires
  after 100 ms, releases RESERVED data, reports one Rx `E_NOT_OK`, and does
  not publish partial READY data.
- **Actual start:** tick 1, reservation active.
- **Actual restart:** valid CF1 at tick 100, 13 payload bytes accumulated,
  reservation still active.
- **Actual expiry:** tick 200, elapsed 100 from CF1; Rx final count 1/result
  `E_NOT_OK`; reservation 0; `CopyRxData=0`; partial READY count 0; abort
  reason 6 = `N_CR_TIMEOUT`.
- **Result:** **PASS**.
- **Evidence:** `EVIDENCE T08` lines in
  [protocol_trace.log](raw/protocol_trace.log).

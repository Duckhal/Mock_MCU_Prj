# T07 - N_Bs timeout and same-tick CTS boundary

- **Phase / environment:** Phase 2, deterministic 1 ms host tick.
- **Config:** Data CAN ID `0x650`, FC CAN ID `0x658`, DLC 8,
  `N_Bs=100 ms`.
- **Setup:** confirm a 20-byte FF at tick 1, then suppress CTS.
- **Expected:** no timeout at elapsed 99 ms; timeout at elapsed 100 ms; no CF
  before CTS. A CTS dispatched at the 100 ms boundary before MainFunction must
  cancel the old timeout.
- **Actual at tick 100:** elapsed 99, one Data frame (FF), zero Tx final
  callbacks.
- **Actual at tick 101:** elapsed 100, still one Data frame, Tx final count 1,
  result `E_NOT_OK`; abort reason 5 = `N_BS_TIMEOUT`.
- **Boundary recovery:** CTS dispatched at fixture tick 100 before the next
  MainFunction; tick 101 requested CF1 and produced no failure callback.
- **Result:** **PASS**.
- **Evidence:** `EVIDENCE T07` lines in
  [protocol_trace.log](raw/protocol_trace.log).

# T13 - N_As timeout and late confirmation

- **Phase / environment:** Phase 2, deterministic 1 ms host tick.
- **Config:** Data CAN ID `0x650`, DLC 8, `N_As=100 ms`.
- **Setup:** Data frame accepted at tick 1; suppress its matching local
  confirmation for 100 ms.
- **Expected:** one final Tx `E_NOT_OK`; accepted Data resource stays locked;
  new request is rejected without another final callback; late confirmation
  only unlocks the resource; a later new request is accepted.
- **Actual at tick 100:** elapsed 99 and zero final callbacks.
- **Actual at tick 101:** elapsed 100, Tx final count 1/result `E_NOT_OK`;
  abort reason 3 = `N_AS_TIMEOUT`; new request returned `E_NOT_OK`.
- **After late confirmation:** final callback count remained 1, no success was
  reported for the old frame, and a new request returned `E_OK`.
- **Result:** **PASS**.
- **Evidence:** `EVIDENCE T13` lines in
  [protocol_trace.log](raw/protocol_trace.log).

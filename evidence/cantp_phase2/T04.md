# T04 - STmin and block-boundary timing

- **Phase / environment:** Phase 2, deterministic 1 ms host tick.
- **Config:** Data CAN ID `0x650`, FC CAN ID `0x658`, DLC 8, BS 4,
  STmin 5 ms. The fixture confirms each accepted Data frame at its recorded
  request tick.
- **Setup:** T03 62-byte transfer; record each CF request tick and CTS count.
- **Expected:** every adjacent CF request after CF1 is at least 5 ms after the
  prior CF confirmation, including CF4 to CF5; two CTS gates.
- **Actual CF ticks:** CF1 `2`, CF2 `7`, CF3 `12`, CF4 `17`, CF5 `22`,
  CF6 `27`, CF7 `32`, CF8 `37`.
- **Actual deltas:** every adjacent pair CF1-CF2 through CF7-CF8 was exactly
  5 ms. CF4-CF5 was 5 ms across the second CTS gate.
- **Actual FC count:** two CTS frames.
- **Result:** **PASS**.
- **Evidence:** `EVIDENCE T04` lines in
  [protocol_trace.log](raw/protocol_trace.log).

# T02 - 20-byte multi-frame N-SDU

- **Phase / environment:** Phase 1, deterministic host fixture, 1 ms tick.
- **Config:** Data CAN ID `0x650`, FC CAN ID `0x658`, DLC 8, BS 4,
  STmin 5 ms.
- **Input:** payload `00..13`.
- **Expected:** FF + CF1 + CF2 and one CTS; committed offsets 6, 13 and 20;
  one full 20-byte application copy.
- **Actual Data frames:** `10 14 00 01 02 03 04 05` at tick 1,
  `21 06 07 08 09 0A 0B 0C` at tick 2, and
  `22 0D 0E 0F 10 11 12 13` at tick 7.
- **Actual FC:** `30 04 05 00 00 00 00 00`; one FC frame.
- **Actual commits:** Tx confirmation offsets 6, 13 and 20.
- **Actual callbacks/payload:** `CopyTxData=1`; Tx final count 1/result
  `E_OK`; `StartOfReception=1`, `CopyRxData=1`; Rx final count 1/result
  `E_OK`; length 20 and payload `00..13` matched.
- **Actual queue:** READY before final indication, then consumed as an
  identical 20-byte payload.
- **Result:** **PASS**.
- **Evidence:** `EVIDENCE T02` lines in
  [protocol_trace.log](raw/protocol_trace.log) and
  [routing_trace.log](raw/routing_trace.log).

# T14 - Valid FF replaces an active receive session

- **Phase / environment:** Phase 3, deterministic 1 ms host tick.
- **Setup:** receive 20 bytes of an active 62-byte session, then inject a new
  valid 62-byte FF while the FC resource is idle and complete the new session.
- **Expected:** fail and release the old RESERVED session exactly once, reserve
  a clean slot for the new session, publish only the new payload, and report
  the new session successful once.
- **Actual:** old session stopped at 20 bytes; old final result `E_NOT_OK` and
  release count 1; new start count 1; new final result `E_OK`; one READY slot
  contained the exact new payload. Callback order was `SISCI`; abort reason 10
  = `CANTP_ABORT_RX_REPLACED`.
- **Result:** **PASS**.
- **Evidence:** `EVIDENCE T14` in
  [phase3_trace.log](raw/phase3_trace.log).

# T12 - Atomic 62-byte receive publication

- **Phase / environment:** Phase 3, deterministic 1 ms host tick.
- **Setup:** receive a valid 62-byte FF/CF sequence through completion.
- **Expected:** call `StartOfReception` once, copy the complete N-SDU once,
  mark its slot READY before the final `RxIndication(E_OK)`, and preserve the
  exact payload.
- **Actual:** start count 1; copy count 1; final Rx count 1/result `E_OK`;
  length 62; exact payload match; callback order
  `START -> COPY/READY -> INDICATION`.
- **Result:** **PASS**.
- **Evidence:** `EVIDENCE T12` in
  [phase3_trace.log](raw/phase3_trace.log).

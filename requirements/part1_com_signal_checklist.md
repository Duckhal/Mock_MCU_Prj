# Checklist Part 1 — COM Signal Communication Model

Trạng thái review ngày **2026-09-18: 36/78 mục đạt, 42/78 mục còn mở**. Mỗi mục có thêm kết quả review ngay sau trường **Bằng chứng**. `[x]` nghĩa là bằng chứng hiện có đủ cho đúng phạm vi mục đó; `[ ]` nghĩa là thiếu test/trace/hiện vật hoặc source chưa đáp ứng. Kết quả host không xác nhận timing thực, CAN bus hai board hay mọi cấu hình có thể có.

Nguồn: **A** = [Assignment Part 1](assignment_part1_com_signal.md) (yêu cầu chính); **N** = [Part 1 Architecture Notes](part1_architecture_notes.md) (giải thích thiết kế); **S** = [COM Internal Scheduler Tip](Com_Internal_Scheduler_Tip.txt) (khuyến nghị bố trí lịch). Số `§` chỉ mục trong A/N. Các mục ghi **Khuyến nghị** không phải điều kiện bắt buộc của Part 1. Một bằng chứng hợp lệ có thể là test deterministic trên host, trace debugger/board, bảng cấu hình đối chiếu, sơ đồ hoặc đoạn giải thích có thể review; cần ghi rõ test chạy với cấu hình nào.

### Phạm vi bằng chứng đã kiểm lại

Đường dẫn ngắn trong kết quả review chỉ các file ở [COM](../drivers/can/com/), [PduR](../drivers/can/pdur/), [CanIf](../drivers/can/canif/), [CanDrv](../drivers/can/can_driver/) và các test ở `tests/com/`, `tests/canif/`, `tests/can_driver/`. Số dòng là vị trí tại thời điểm review. Đã chạy lại từ source hiện tại:

| Phạm vi | Kết quả | Log |
|---|---|---|
| CanDrv fixture + production config + ARM compile | 11 nhóm, 14 config lỗi, 9 regression production: PASS | `build/can_driver/verification.log` |
| CanIf fixture + production config + ARM compile | 7 nhóm và smoke test: PASS | `build/canif/verification.log` |
| COM host + config fixtures, GCC `-Wall -Wextra -Werror` | pack/Rx/retry/drop PASS; 12 config lỗi bị từ chối | `build/com/review_verification.log` |

`tests/*` đang bị `.gitignore` bỏ qua, nên các test host trên là bằng chứng **trong workspace này**; cần đưa test vào quản lý phiên bản nếu muốn người khác chạy lại từ checkout sạch. Không dùng ELF/log build cũ để đánh dấu full-stack hoặc board pass.

| Luồng cấu hình hiện có | Ánh xạ kiểm từ source |
|---|---|
| Tx VehicleStatus | COM I-PDU local 0, Group 0, Signal 0/1/2, GlobalPduId `0x0010` → PduR Tx route 0→0 → CanIf Tx L-PDU 0, CAN ID `0x100`, HTH 0 → CanDrv Controller 0 / MB8; length 8, period 10 tick, offset 1 tick, maxRetries 3. |
| Rx VehicleStatus | CanDrv Controller 0 / HRH 1 / MB9, CAN ID `0x100` → CanIf Rx L-PDU 0 → PduR Rx route 0→1 → COM I-PDU local 1, Group 1, Signal 3/4/5, GlobalPduId `0x0010`. |

Bảng này là đối chiếu cấu hình tĩnh, không phải trace runtime xuyên tầng. src/main.c hiện chỉ chạy ứng dụng COM hai board: Tx cập nhật Signal bằng Com_SendSignal; Rx đọc bằng Com_ReceiveSignal. Mã test nằm ngoài main.

## 1. Phạm vi, sở hữu và định danh

- [ ] **P01 — Giữ đúng chuỗi Tx:** Signal → Signal Group → I-PDU → PduR → CanIf Tx L-PDU → CAN ID + HTH → CanDrv Hardware Object → Controller. **Bằng chứng:** sơ đồ cấu hình với ID/handle thực tế và một trace Tx xuyên tầng. **Nguồn:** A §1, §39. **Review 2026-09-18:** Chưa đủ — Có đường gọi trong source, nhưng chưa có trace full-stack chạy lại từ Signal tới controller; mode hai-board dùng CanIf trực tiếp.
- [ ] **P02 — Giữ đúng chuỗi Rx:** Controller → Rx Hardware Object/HRH → HRH + CAN ID → CanIf Rx L-PDU → PduR → I-PDU → Signal Group → Signal. **Bằng chứng:** sơ đồ cấu hình và một trace Rx có giá trị Signal được giải mã. **Nguồn:** A §31, §39. **Review 2026-09-18:** Chưa đủ — Có đường gọi Rx trong source, nhưng chưa có trace full-stack chạy lại qua COM trên board/current host harness.
- [x] **P03 — Tách quyền sở hữu cấu hình:** COM sở hữu Signal/Group/I-PDU, slot/Update Bit và lịch; PduR sở hữu route; CanIf sở hữu ánh xạ CAN ID và L-PDU; CanDrv sở hữu HOH/Controller/tài nguyên vật lý. **Bằng chứng:** bảng ownership dẫn tới struct/config API từng module; review không thấy tầng khác sửa dữ liệu thuộc sở hữu COM. **Nguồn:** A §4–6; N §21–23. **Review 2026-09-18:** Đạt — Com_Types.h/Com_Cfg.c, PduR_Types.h/PduR_Cfg.c, CanIf_Types.h/CanIf_Cfg.c và Can_Types.h/Can_Cfg.c phân trường theo đúng tầng; PduR.c:14–78 chỉ chuyển local handles/payload.
- [x] **P04 — Mỗi Signal thuộc đúng một Group; mỗi Group thuộc đúng một I-PDU; mỗi I-PDU chứa đúng một Group.** Tra cứu Signal → Group → I-PDU, không bắt buộc Signal có `IPduRef` trực tiếp. **Bằng chứng:** bảng liên kết cấu hình và test từ chối thiếu/trùng liên kết. **Nguồn:** A §2, §9–10, §36; N §2–3. **Review 2026-09-18:** Đạt — Com.c:138–209 kiểm tra owner/membership; test_com_config.c:36–68 có các fixture Group rỗng, Group trùng và Group dùng chung (PASS).
- [x] **P05 — Phân biệt local PduId và GlobalPduId:** local handle chỉ có nghĩa trong module; GlobalPduId là định danh logic để tương quan/trace toàn hệ thống. **Bằng chứng:** bảng đối chiếu local handle ở COM/PduR/CanIf với GlobalPduId cho ít nhất một luồng Tx và một luồng Rx. **Nguồn:** A §3.5, §10, §17; N §30. **Review 2026-09-18:** Đạt — Com_Cfg.c:57–75, PduR_Cfg.c:7–25, CanIf_Cfg.c:4–22 ánh xạ local COM Tx0/Rx1, PduR0 và CanIf Tx0/Rx0 tới Global 0x0010.
- [ ] **P06 — Direct CAN Binding bắt buộc:** một GlobalPduId logic ánh xạ duy nhất tới CanIf L-PDU/CAN ID tương ứng; GlobalPduId được suy ra từ cấu hình, không chèn thêm byte vào payload CAN. **Bằng chứng:** bảng binding hai chiều và test/trace dựng lại GlobalPduId từ TxPduId hoặc `(HRH, CAN ID)`, kèm kiểm tra DLC/payload. **Nguồn:** A §17–18, §24, §36; N §31–33. **Review 2026-09-18:** Chưa đủ — Bảng cấu hình hiện ánh xạ 0x0010→0x100; thiếu kiểm tra uniqueness xuyên module và trace dựng GlobalPduId từ frame thực.
- [x] **P07 — Không đưa thuật ngữ mailbox/tài nguyên vật lý vào API tầng trên:** HTH/HRH là handle tới HOH, từ đó CanDrv xác định Controller; PduR không đọc Signal, U bit, HTH/HRH hoặc Controller. **Bằng chứng:** review header/config và trace route chỉ dùng local PDU handles. **Nguồn:** A §17, §19–24; N §23–28. **Review 2026-09-18:** Đạt — PduR.c:14–78 không đọc slot/U/HOH; CanIf.c:231–275 dùng HTH; Can.c:138–162 resolve HOH→Controller.

## 2. Mô hình COM và kiểm tra cấu hình

- [ ] **C01 — Signal và slot:** mỗi Signal có ID, kiểu dữ liệu, `SlotStartBit`, `SlotLength`; bit 0 của slot là U, các bit còn lại là payload; `PayloadBitLength = SlotLength - 1`. **Bằng chứng:** struct/config và test encode/decode cho slot 8/16/24/32 bit. **Nguồn:** A §7–8; N §4–7. **Review 2026-09-18:** Chưa đủ — Com_SignalConfigType không có trường data_type; test hiện chỉ phủ slot 8/16 bit, chưa có 24/32 bit.
- [ ] **C02 — SlotStartBit chia hết cho 8.** **Bằng chứng:** test cấu hình hợp lệ và test từ chối start bit lệch byte. **Nguồn:** A §7, §36 COM. **Review 2026-09-18:** Chưa đủ — Com.c có điều kiện modulo, nhưng test_com_config.c chưa inject start bit lệch byte.
- [ ] **C03 — SlotLength chia hết cho 8.** **Bằng chứng:** test từ chối slot dài 7/9/15 bit. **Nguồn:** A §7, §36 COM. **Review 2026-09-18:** Chưa đủ — Com.c có điều kiện modulo, nhưng test chỉ dùng length=7; thiếu case 9/15 bit tách khỏi điều kiện length<8.
- [ ] **C04 — SlotLength tối thiểu 8 bit.** **Bằng chứng:** test từ chối 0 bit và chấp nhận 8 bit. **Nguồn:** A §36 COM. **Review 2026-09-18:** Chưa đủ — Com.c từ chối <8, nhưng chưa có test length=0 và chấp nhận 8 bit theo đúng cặp biên.
- [x] **C05 — Các slot trong cùng I-PDU không chồng lấn.** **Bằng chứng:** fixture hai slot giao nhau bị từ chối, hai slot liền kề được chấp nhận. **Nguồn:** A §36 COM. **Review 2026-09-18:** Đạt — Com.c:193–207 kiểm tra giao khoảng; test_com_config.c:44–47 scenario 1 từ chối overlap, Com_Cfg.c:18–43 có các slot liền kề (PASS).
- [x] **C06 — Mọi slot nằm trọn trong chiều dài I-PDU.** **Bằng chứng:** test slot kết thúc đúng biên và vượt biên một byte. **Nguồn:** A §36 COM. **Review 2026-09-18:** Đạt — Com.c:184–190 kiểm tra biên I-PDU; test_com_config.c:46 scenario 3 vượt 64 bit bị từ chối (PASS).
- [x] **C07 — Giá trị Signal vừa `SlotLength - 1` bit payload, dù kiểu C có thể rộng hơn.** **Bằng chứng:** test giới hạn min/max và giá trị vượt ngưỡng, ví dụ slot 16 bit từ chối `0x8000`. **Nguồn:** A §7, §36 COM; N §7. **Review 2026-09-18:** Đạt — Com.c:251–265 giới hạn theo slotLength−1; test_com.c:26–34 chấp nhận 32767/127, từ chối 32768/128 (PASS).
- [x] **C08 — Group không rỗng.** **Bằng chứng:** test từ chối Group có 0 Signal. **Nguồn:** A §36 Signal Group. **Review 2026-09-18:** Đạt — Com.c:166–172 từ chối Group rỗng; test_com_config.c:48 scenario 5 (PASS).
- [ ] **C09 — Không có Signal thuộc hai Group.** **Bằng chứng:** fixture cùng Signal xuất hiện ở hai Group bị từ chối. **Nguồn:** A §36 Signal Group. **Review 2026-09-18:** Chưa đủ — Com.c kiểm membership, nhưng chưa có fixture cùng Signal trong hai Group khác nhau.
- [x] **C10 — Không có Group thuộc hai I-PDU.** **Bằng chứng:** fixture cùng Group tham chiếu từ hai I-PDU bị từ chối. **Nguồn:** A §36 Signal Group. **Review 2026-09-18:** Đạt — Com.c:145–155 từ chối Group dùng chung; test_com_config.c:50 scenario 7 (PASS).
- [ ] **C11 — Mỗi I-PDU có đúng một SignalGroupRef.** **Bằng chứng:** test từ chối I-PDU không có hoặc có nhiều Group. **Nguồn:** A §10, §36 I-PDU. **Review 2026-09-18:** Chưa đủ — Trường GroupRef đơn trị và code kiểm tồn tại; thiếu fixture I-PDU tham chiếu Group không tồn tại.
- [x] **C12 — I-PDU có local PduId, GlobalPduId, direction, length, SignalGroupRef, period/offset/max_retries cho Tx và runtime buffer.** **Bằng chứng:** bảng/struct cấu hình và test khởi tạo Tx/Rx. **Nguồn:** A §10–11, §35. **Review 2026-09-18:** Đạt — Com_Types.h:30–43, Com_Cfg.c:57–75 và Com.c:6–20 có I-PDU config cùng runtime buffer; Com_Init test trong test_com.c:21–62 (PASS).
- [ ] **C13 — Mỗi I-PDU có GlobalPduId; mỗi định danh logic là duy nhất trong mô hình truyền thông toàn hệ thống.** Có thể có bản đại diện Tx/Rx cho cùng thông điệp logic; kiểm tra uniqueness phải theo thông điệp/binding, không nhầm local handle với định danh logic. **Bằng chứng:** bảng ánh xạ toàn hệ thống, test từ chối hai thông điệp khác nhau dùng cùng GlobalPduId. **Nguồn:** A §3.5, §36 I-PDU; N §30. **Review 2026-09-18:** Chưa đủ — Com.c chỉ so GlobalPduId trong cùng direction; chưa có validator hệ thống phân biệt cùng thông điệp Tx/Rx với hai thông điệp khác nhau.
- [ ] **C14 — Chu kỳ Tx hợp lệ và là bội số tick COM; offset cũng là bội số tick COM.** **Bằng chứng:** test chu kỳ 0/không chia hết bị từ chối, chu kỳ và offset hợp lệ được đổi thành số tick đúng. **Nguồn:** A §12, §36 I-PDU. **Review 2026-09-18:** Chưa đủ — Config lưu tick nên bội số 1 ms được ngầm hóa; chưa có validator ms→tick/test period/offset không chia hết. Com.c còn từ chối offset=0.
- [x] **C15 — `max_retries` là số nguyên không âm biểu diễn được bởi bộ đếm runtime.** **Bằng chứng:** test biên 0, tối đa của kiểu đếm và cấu hình vượt giới hạn bị từ chối (nếu parser cho phép). **Nguồn:** A §13–14, §36 I-PDU. **Review 2026-09-18:** Đạt — Com_Types.h:40 dùng uint8_t maxRetries, Com.c:309–318 dùng uint8_t retryCount; 0..255 biểu diễn được, không âm (kiểm tra kiểu tĩnh).
- [x] **C16 — Tách cấu hình tĩnh khỏi trạng thái động:** period/offset/max_retries là cấu hình; counter/pending/retry_count/buffer là runtime. **Bằng chứng:** đối chiếu struct và test init/reset không làm thay đổi bảng cấu hình. **Nguồn:** A §11, §35; N §8–10. **Review 2026-09-18:** Đạt — Com.c:6–20 và :224–248 tách config const khỏi counter/pending/retry/buffer runtime; test_com.c:21–86 gọi Com_Init nhiều lần (PASS).

## 3. COM Tx, scheduler và Update Bit

- [x] **T01 — `Com_SendSignal()` tra Signal → Group → I-PDU, kiểm tra ID/con trỏ/giá trị, ghi `(Value << 1) | 1` vào slot và không tự phát CAN.** **Bằng chứng:** test invalid input/range và test buffer có U=1 nhưng chưa gọi PduR/CanIf trước tick due. **Nguồn:** A §15; N §8. **Review 2026-09-18:** Đạt — Com.c:87–109 và :251–265 chỉ ghi buffer/U, không gọi PduR; test_com.c:21–39 thử max/min và không có call trước tick (PASS).
- [ ] **T02 — Com_MainFunctionTx() được gọi mỗi 1 ms khi Tx được kích hoạt.** **Bằng chứng:** src/main.c dùng SysTick 1000 Hz, gọi Can Write → Can Read → COM Tx theo từng tick ở Tx; tests/com_stack/test_main_scheduler.c kiểm chứng thứ tự và tick catch-up (PASS). Chưa đo timing thực trên board, nên phần wall-clock còn mở. **Nguồn:** A §12, §14 COM-DYN-01; N §9–10.
- [x] **T03 — Init mỗi Tx I-PDU với `counter=initialOffsetTicks`, `pending=false`, `retry_count=0`.** **Bằng chứng:** test snapshot runtime ngay sau `Com_Init`. **Nguồn:** A §13. **Review 2026-09-18:** Đạt — Com.c:224–248 reset runtime bằng memset rồi nạp counter=initialOffsetTicks; test_com.c:35–40 có attempt đầu ở tick 1 (PASS).
- [ ] **T04 — Mỗi tick giảm counter nếu >0; khi tới 0 thì đặt pending nếu chưa pending, reset retry_count cho occurrence mới và nạp lại period dù occurrence cũ còn pending.** **Bằng chứng:** bảng trace tick qua offset, kỳ kế tiếp và trường hợp BUSY kéo dài qua mốc period. **Nguồn:** A §13–14 COM-DYN-02/03/07/08. **Review 2026-09-18:** Chưa đủ — Source nạp lại counter đúng; thiếu test pending kéo dài qua mốc period để chứng minh coalescing/reload.
- [x] **T05 — Mỗi I-PDU pending được thử gửi đúng một lần trong mỗi lần gọi MainFunctionTx; thất bại thử lại ở tick kế tiếp.** **Bằng chứng:** fake PduR đếm call theo tick BUSY→BUSY→OK, không có vòng chờ. **Nguồn:** A §13–14 COM-DYN-04/05. **Review 2026-09-18:** Đạt — Com.c:285–325 có đúng một PduR call mỗi vòng I-PDU/tick; test_com.c:64–77 đếm BUSY/retry ở tick kế (PASS).
- [ ] **T06 — Xử lý I-PDU theo thứ tự cấu hình và tiếp tục I-PDU sau khi một PDU thất bại.** **Bằng chứng:** test hai PDU cùng due, PDU đầu BUSY và PDU sau vẫn có transmit attempt cùng tick theo đúng thứ tự. **Nguồn:** A §14 COM-DYN-05/06; N §12, §14. **Review 2026-09-18:** Chưa đủ — Source lặp theo thứ tự bảng; production chỉ có một Tx I-PDU, chưa có test hai PDU cùng due và một PDU BUSY.
- [ ] **T07 — Trạng thái pending gộp các kỳ bỏ lỡ, không tạo hàng đợi; cập nhật Signal mới trong lúc pending được dùng cho lần chấp nhận tiếp theo.** **Bằng chứng:** test BUSY qua nhiều kỳ, thay Signal giữa chừng, chỉ một request thành công với giá trị mới nhất. **Nguồn:** A §14 COM-DYN-07/11; N §16. **Review 2026-09-18:** Chưa đủ — Test có latest value khi pending ngắn; thiếu test BUSY qua nhiều kỳ nominal để chứng minh không tạo queue.
- [ ] **T08 — Lịch định kỳ không trôi theo lúc request được chấp nhận:** ví dụ kỳ 10 ms, request ở 10 ms BUSY rồi được chấp nhận 12 ms, kỳ tiếp theo vẫn 20 ms. **Bằng chứng:** trace due/accept với đồng hồ giả. **Nguồn:** A §14 COM-DYN-08; N §17. **Review 2026-09-18:** Chưa đủ — Source nạp lại counter từ mốc due; thiếu test accepted trễ t=12 rồi due tiếp ở t=20/21.
- [ ] **T09 — `max_retries=N` cho tối đa `1+N` lần thử; lần thử đầu không tính retry.** **Bằng chứng:** test N=0 và N=3 đếm số lần gọi PduR khi luôn E_NOT_OK. **Nguồn:** A §14 COM-DYN-09; N §15. **Review 2026-09-18:** Chưa đủ — Test hiện kiểm maxRetries=3; thiếu case maxRetries=0 và biên số attempt.
- [x] **T10 — Hết retry chỉ bỏ occurrence hiện tại, đặt pending=false/retry_count=0; I-PDU vẫn hoạt động ở kỳ nominal sau.** **Bằng chứng:** trace BUSY đến drop rồi tiếp tục đến kỳ kế tiếp; có counter/log drop. **Nguồn:** A §14 COM-DYN-10, §25. **Review 2026-09-18:** Đạt — Com.c:313–322 reset pending/retry khi drop; test_com.c:64–85 thấy 4 lần thử, 1 drop và gửi lại kỳ t=11 (PASS).
- [x] **T11 — Khi PduR chấp nhận (`E_OK`), xóa pending/retry_count và bit U của mọi slot thuộc I-PDU, giữ nguyên payload.** **Bằng chứng:** byte snapshot trước/sau E_OK với nhiều Signal; test xác nhận payload không đổi. **Nguồn:** A §13, §16; N §18. **Review 2026-09-18:** Đạt — Com.c:299–307 và :211–222 chỉ xóa bit0 khi E_OK; test_com.c:43–62 chứng minh cả 3 U về 0, payload nhận vẫn đúng (PASS).
- [x] **T12 — Khi E_NOT_OK hoặc drop, giữ nguyên U bit và buffer để lần sau dùng dữ liệu mới nhất.** **Bằng chứng:** byte snapshot sau BUSY/drop và test kỳ nominal sau. **Nguồn:** A §16, §25, §14 COM-DYN-11. **Review 2026-09-18:** Đạt — Com.c:308–322 không xóa U khi fail/drop; test_com.c:64–85 kiểm tra bytes[2]=5 ở kỳ sau (U còn 1) (PASS).
- [x] **T13 — Phân biệt request được Can_Write nhận với Tx vật lý hoàn tất và với xác nhận từ ECU đích.** **Bằng chứng:** trace trạng thái request/TxConfirmation tách biệt; tài liệu test không coi `E_OK` là remote ACK. **Nguồn:** A §26, §28–29; N §19. **Review 2026-09-18:** Đạt — Can.c:465–521 trả CAN_OK khi request được nạp, :523–556 mới gọi TxConfirmation; tests/canif/test_canif.c:151–170 xác nhận accepted chưa có confirmation (PASS host).

## 4. PduR, CanIf và CanDrv

- [ ] **R01 — PduR route chỉ dùng local source/destination PDU handles, không sửa payload; source PDU tồn tại.** **Bằng chứng:** test route Tx/Rx với byte payload so sánh nguyên vẹn và fixture source không tồn tại bị từ chối. **Nguồn:** A §17, §36 PduR. **Review 2026-09-18:** Chưa đủ — PduR.c forward payload và từ chối unknown handle lúc gọi, nhưng không validate sourcePduId trong bảng route lúc init.
- [ ] **R02 — Destination PDU của từng route tồn tại.** **Bằng chứng:** fixture route destination sai bị từ chối. **Nguồn:** A §36 PduR. **Review 2026-09-18:** Chưa đủ — PduR không có bước init/validate destinationPduId của bảng route.
- [ ] **R03 — Mỗi route truy được đúng một GlobalPduId; Direct Binding không có mapping mơ hồ từ GlobalPduId tới CanIf L-PDU/CAN ID.** **Bằng chứng:** bảng route/binding và test từ chối thiếu hoặc trùng mapping. **Nguồn:** A §17, §36 PduR. **Review 2026-09-18:** Chưa đủ — PduR không kiểm route GlobalPduId với COM/CanIf; duplicate/missing binding chưa bị từ chối lúc init.
- [x] **R04 — CanIf Tx ánh xạ TxPduId → CAN ID + HTH rồi gọi Can_Write bằng HTH; CAN ID hợp lệ.** **Bằng chứng:** test ghi nhận tham số Can_Write; fixture CAN ID ngoài profile được từ chối. **Nguồn:** A §18, §36 CanIf Tx. **Review 2026-09-18:** Đạt — CanIf.c:139–176 kiểm CAN ID, :231–275 ánh xạ TxPduId→ID/HTH; test_canif.c:107–130, :137–172 kiểm ID sai và tham số Can_Write (PASS).
- [x] **R05 — HthRef tồn tại và trỏ tới HOH kiểu Tx.** **Bằng chứng:** test HTH thiếu, HTH kiểu Rx bị từ chối; cấu hình hợp lệ chọn đúng Controller. **Nguồn:** A §19–20, §36 CanIf Tx. **Review 2026-09-18:** Đạt — CanIf.c:75–107, :150–159 kiểm HTH/Tx/controller; test_canif.c:116–118, :125–126 kiểm sai loại/thiếu/controller thiếu (PASS).
- [x] **R06 — CanIf Rx tra khóa `(HRH, CAN ID)` duy nhất để tìm Rx L-PDU và chuyển payload lên PduR.** **Bằng chứng:** test cùng HRH nhận nhiều CAN ID, và trùng cặp HRH/CAN ID bị từ chối. **Nguồn:** A §23–24, §36 CanIf Rx. **Review 2026-09-18:** Đạt — CanIf.c:123–137 và :178–216 dùng khóa HRH+CAN ID; test_canif.c:197–222 thử HRH1 với ID 0x321/0x200 và scenario 8 trùng khóa (PASS).
- [x] **R07 — CAN ID của Rx hợp lệ; HrhRef tồn tại và trỏ tới HOH kiểu Rx.** **Bằng chứng:** fixture CAN ID sai/HRH thiếu/HRH kiểu Tx bị từ chối. **Nguồn:** A §36 CanIf Rx. **Review 2026-09-18:** Đạt — CanIf.c:178–216 kiểm ID/HRH/Rx type; test_canif.c:121–123, :125–126 có fixture từ chối (PASS).
- [ ] **R08 — Driver báo Rx qua `CanIf_RxIndication(Hrh, RxPdu)`, trong đó RxPdu có CAN ID, length, data; HRH xác định Controller nên API training không cần ControllerId riêng.** **Bằng chứng:** test callback từ hai HRH/controller giả lập và kiểm tra tuple/payload. **Nguồn:** A §21–24; N §26–27. **Review 2026-09-18:** Chưa đủ — Callback HRH+RxPdu có trong Can.c, nhưng test chỉ một Controller/HRH; thiếu fixture hai Controller.
- [ ] **R09 — Trong một CanDrv instance, CanObjectId duy nhất cho cả HTH và HRH trong cùng không gian ID.** **Bằng chứng:** test config trùng Tx–Tx, Rx–Rx, Tx–Rx đều bị từ chối. **Nguồn:** A §33, §36 CanDrv. **Review 2026-09-18:** Chưa đủ — Can.c có shared namespace; test Tx–Rx và cùng loại Tx, chưa có fixture Rx–Rx trùng ID riêng.
- [x] **R10 — Mỗi HOH tham chiếu đúng một Controller; HOH xác định duy nhất Hardware Object và Controller.** **Bằng chứng:** test controller thiếu/trùng tham chiếu và lookup HOH ID ra đúng object/controller. **Nguồn:** A §19–20, §33, §36 CanDrv. **Review 2026-09-18:** Đạt — Can.c:118–239 kiểm controller reference/duplicate và resolve HOH; test_can_driver.c:513–608 kiểm missing/duplicate controller và sparse handles (PASS).
- [ ] **R11 — Mô hình hỗ trợ nhiều Controller: HTH/HRH của hai Controller khác nhau vẫn resolve duy nhất, kể cả khi bản phần cứng đang triển khai chỉ có CAN0.** **Bằng chứng:** bảng/fixture hai controller với HTH/HRH riêng và test lookup; ghi rõ giới hạn runtime trên board nếu chưa hỗ trợ CAN1. **Nguồn:** A §20, §38; N §24–26. **Review 2026-09-18:** Chưa đủ — Can.c:173–239 yêu cầu CAN_NUM_CONTROLLERS==1 và chỉ CAN0; multi-controller config bị từ chối.
- [ ] **R12 — Khi Can_Write trả CAN_BUSY, CanIf trả E_NOT_OK và PduR truyền nguyên thất bại lên COM.** **Bằng chứng:** test fault injection CAN_BUSY ở driver và assert kết quả từng tầng; COM còn pending/U=1. **Nguồn:** A §25. **Review 2026-09-18:** Chưa đủ — Có CanIf unit CAN_BUSY và COM unit E_NOT_OK riêng; thiếu fault-injection xuyên CanDrv→CanIf→PduR→COM kiểm pending/U.
- [x] **R13 — Nếu Tx completion dùng polling, `Can_MainFunction_Write()` nhận ra hoàn tất và gọi `CanIf_TxConfirmation()` cho đúng request; Rx polling được phục vụ nếu dùng.** **Bằng chứng:** test fake hardware accepted→busy→completed và callback đúng một lần; trace Rx polling nếu áp dụng. **Nguồn:** A §26–27; N §19–20. **Review 2026-09-18:** Đạt — Can.c:523–556 gọi confirmation đúng một lần sau hoàn tất; :558–632 poll Rx; test_can_driver.c:363–450, :579–608 kiểm callback/HRH (PASS).

## 5. Tích hợp, lịch tham khảo và trace

- [ ] **I01 — Có ca tích hợp Tx từ Com_SendSignal qua PduR/CanIf/CanDrv đến CAN ID+HTH+Controller đúng cấu hình.** **Bằng chứng:** test full-stack hoặc board trace gồm local handles, GlobalPduId, CAN ID và byte payload. **Nguồn:** A §30, §39. **Review 2026-09-18:** Chưa đủ — Chưa chạy lại full-stack host mô phỏng; main hai-board phát trực tiếp qua CanIf, không đi COM/PduR Tx.
- [ ] **I02 — Có ca tích hợp Rx từ HRH+CAN ID qua CanIf/PduR/COM đến Signal, kiểm tra U bit và payload.** **Bằng chứng:** inject frame rồi đọc Signal, đối chiếu dữ liệu từng tầng. **Nguồn:** A §31, §39. **Review 2026-09-18:** Chưa đủ — Chưa chạy lại trace Rx xuyên toàn stack hiện tại; main hai-board chỉ đọc PduR snapshot, không đọc COM Signal.
- [ ] **I03 — Có trace end-to-end GlobalPduId cho Tx và Rx dù ID này không xuất hiện trong payload Direct Binding.** **Bằng chứng:** bảng `GlobalPduId ↔ local handles ↔ (HRH, CAN ID)/HTH` và log của một frame hai chiều. **Nguồn:** A §3.5, §17–18, §24, §38; N §30–34. **Review 2026-09-18:** Chưa đủ — Config suy ra 0x0010, nhưng log runtime chưa mang GlobalPduId và chưa có trace hai chiều.
- [x] **I04 — Thứ tự polling trong tick 1 ms:** Can_MainFunction_Write(), Can_MainFunction_Read(), rồi Com_MainFunctionTx() khi ở Tx. **Bằng chứng:** src/main.c thực hiện thứ tự này; tests/com_stack/test_main_scheduler.c kiểm WRC khi backlog 3 tick (PASS). Chưa đo timing vật lý trên board (T02 còn mở). **Nguồn:** A §27; N §20.
- [ ] **I05 — Khuyến nghị từ scheduler tip:** chọn chu kỳ là bội của khoảng chung (ví dụ 10 ms) và offset khác nhau 1–9 ms trong cửa sổ đó để tách phase due. **Bằng chứng:** bảng cấu hình period/offset và bảng due cho các I-PDU; ví dụ A=20/1, B=20/3, C=30/5 có due 1/21/41, 3/23/43, 5/35/65 ms. **Nguồn:** S; A §12; N §11. **Review 2026-09-18:** Chưa đủ — Config hiện chỉ một Tx I-PDU period10/offset1; chưa có bảng/fixture A20/1 B20/3 C30/5.
- [ ] **I06 — Khuyến nghị từ scheduler tip:** kiểm tra offset là phase ban đầu, không phải thứ tự lần truyền; retry có thể phát sinh cùng tick với PDU khác nên bố trí phase không bảo đảm tránh mọi tranh chấp. **Bằng chứng:** trace lịch nominal và test BUSY tạo retry trùng due PDU khác, cả hai vẫn được xử lý không chặn. **Nguồn:** S; A §14 COM-DYN-05/08. **Review 2026-09-18:** Chưa đủ — Chưa có test hai PDU với retry trùng nominal due; chỉ một Tx I-PDU.

## 6. Hồ sơ bàn giao bắt buộc theo A §38

Các mục dưới đây kiểm tra **sự hiện diện của hiện vật để review**; các mục trên kiểm tra tính đúng của hành vi và validation. Có thể dùng chung một file/sơ đồ cho nhiều deliverable nếu nó chỉ ra rõ từng nội dung.

- [ ] **D01 — Signal model.** **Bằng chứng:** đường dẫn định nghĩa kiểu và bảng Signal ID/kiểu/Group. **Review 2026-09-18:** Chưa đủ — Signal model hiện thiếu kiểu dữ liệu trong Com_SignalConfigType; chưa có bảng Signal ID/kiểu hoàn chỉnh.
- [ ] **D02 — Signal Slot model.** **Bằng chứng:** sơ đồ slot, U/payload bit, start/length và ví dụ byte encode. **Review 2026-09-18:** Chưa đủ — Code có slot và U bit, nhưng chưa có hiện vật sơ đồ slot độc lập cùng ví dụ encode 8/16/24/32.
- [x] **D03 — Signal Group model.** **Bằng chứng:** bảng Group → danh sách Signal và ràng buộc không rỗng. **Review 2026-09-18:** Đạt — Com_Types.h:22–28 và Com_Cfg.c:4–15, :45–54 định nghĩa Group0={Signal0,1,2}, Group1={Signal3,4,5}; C08 đã kiểm Group không rỗng.
- [x] **D04 — I-PDU model có GlobalPduId.** **Bằng chứng:** bảng I-PDU ID, Global ID, direction, length, Group và buffer. **Review 2026-09-18:** Đạt — Com_Types.h:30–43, Com_Cfg.c:56–75 có Tx local0/Rx local1 cùng Global 0x0010, direction/length/group; Com.c:15 có runtime buffer.
- [x] **D05 — Direct CAN Binding map.** **Bằng chứng:** bảng GlobalPduId ↔ CanIf L-PDU ↔ CAN ID cho từng thông điệp. **Review 2026-09-18:** Đạt — Bảng binding trong phần đầu checklist review đối chiếu Com_Cfg.c:57–75, PduR_Cfg.c:7–25, CanIf_Cfg.c:4–22: Global0x0010 ↔ Tx/Rx L-PDU0 ↔ CAN0x100.
- [x] **D06 — Cấu hình period/offset/max_retries.** **Bằng chứng:** bảng giá trị Tx I-PDU và tick chuyển đổi. **Review 2026-09-18:** Đạt — Com_Cfg.c:57–75: Tx periodTicks=10, initialOffsetTicks=1, maxRetries=3; base tick dự kiến 1 ms theo Com.h:35.
- [x] **D07 — COM Tx runtime state model.** **Bằng chứng:** sơ đồ/bảng counter, pending, retry_count, buffer và chuyển trạng thái. **Review 2026-09-18:** Đạt — Com.c:6–20 và :285–325 định nghĩa/chuyển counter, pending, retryCount; test_com.c:64–85 kiểm retry/drop.
- [x] **D08 — PduR route model.** **Bằng chứng:** bảng source/destination local handle và GlobalPduId tương ứng. **Review 2026-09-18:** Đạt — PduR_Types.h:6–18 và PduR_Cfg.c:7–25 chứa route Tx COM0→CanIf0, Rx CanIf0→COM1, Global0x0010.
- [x] **D09 — CanIf Tx L-PDU model.** **Bằng chứng:** bảng TxPduId, CAN ID, HTH. **Review 2026-09-18:** Đạt — CanIf_Types.h:7–13 và CanIf_Cfg.c:4–12 có TxPduId0, CAN ID0x100, HTH0.
- [x] **D10 — CanIf Rx L-PDU model.** **Bằng chứng:** bảng RxPduId, HRH, CAN ID. **Review 2026-09-18:** Đạt — CanIf_Types.h:14–20 và CanIf_Cfg.c:13–22 có RxPduId0, CAN ID0x100, HRH1.
- [x] **D11 — CanDrv Hardware Object model.** **Bằng chứng:** bảng HOH ID/type/Controller và tài nguyên vật lý. **Review 2026-09-18:** Đạt — Can_Types.h:46–53 và Can_Cfg.c:5–31 có Controller0, HTH0/MB8 và HRH1/MB9.
- [ ] **D12 — Multiple Controller model.** **Bằng chứng:** sơ đồ hoặc fixture ít nhất hai Controller với HOH riêng, nêu phạm vi runtime đã chạy. **Review 2026-09-18:** Chưa đủ — CanDrv fixed CAN0 và từ chối hai Controller; chưa có fixture multi-controller được chấp nhận.
- [x] **D13 — Configuration Ownership View.** **Bằng chứng:** sơ đồ/bảng mỗi module sở hữu trường cấu hình nào. **Review 2026-09-18:** Đạt — Bảng ownership trong phần đầu checklist review, đối chiếu Com_Cfg.c, PduR_Cfg.c, CanIf_Cfg.c, Can_Cfg.c.
- [ ] **D14 — Building Block View.** **Bằng chứng:** sơ đồ các module và giao diện Tx/Rx giữa chúng. **Review 2026-09-18:** Chưa đủ — Chưa có Building Block View của implementation hiện tại; sơ đồ trong assignment là đề bài.
- [ ] **D15 — Tx Dynamic Behavior View.** **Bằng chứng:** sequence/state diagram cho due, BUSY, retry, E_OK, confirmation. **Review 2026-09-18:** Chưa đủ — Chưa có Tx Dynamic Behavior View/sequence của implementation hiện tại; code/test riêng chưa thay thế hiện vật.
- [ ] **D16 — Rx Runtime View.** **Bằng chứng:** sequence diagram từ Controller/HRH tới Signal. **Review 2026-09-18:** Chưa đủ — Chưa có Rx Runtime View/sequence của implementation hiện tại.
- [ ] **D17 — CAN_BUSY bounded retry/drop behavior.** **Bằng chứng:** test N=0/N>0 với số attempt, pending, U bit và kỳ sau. **Review 2026-09-18:** Chưa đủ — Có test 1+3 retry/drop; thiếu case N=0 và test CAN_BUSY full-stack.
- [ ] **D18 — End-to-End Tx/Rx Trace View dùng GlobalPduId.** **Bằng chứng:** một trace Tx và một trace Rx, có đủ ánh xạ local IDs/CAN ID/HOH. **Review 2026-09-18:** Chưa đủ — Chưa có trace runtime Tx/Rx mang GlobalPduId và local handles/HOH đầy đủ.
- [ ] **D19 — Validation report.** **Bằng chứng:** báo cáo liệt kê toàn bộ mục C/R validation, test pass/fail, config được thử, giới hạn chưa kiểm tra trên board và đường dẫn log. **Review 2026-09-18:** Chưa đủ — Báo cáo validation chưa hoàn chỉnh: thiếu route/cross-layer và các fixture COM biên; bảng review này ghi rõ lỗ hổng nhưng chưa đạt toàn bộ.

## 7. Tiêu chí giải thích theo A §39

- [ ] **A01 — Giải thích ownership:** ai sở hữu ComSignal, ComIPdu, CanIfTxPdu, CAN ID mapping, HTH/HRH, CanController; vì sao CanIf chỉ tham chiếu HTH. **Bằng chứng:** bản trả lời ngắn cho từng câu, đối chiếu bảng P03 và API/config thực tế. **Review 2026-09-18:** Chưa đủ — Chưa có bản giải thích từng câu hỏi ownership được review.
- [ ] **A02 — Giải thích định danh và Rx:** HTH/HRH suy ra Controller thế nào, vì sao Rx API không cần ControllerId, vì sao `(HRH,CAN ID)` nhận diện L-PDU và dựng lại GlobalPduId; phân biệt Global ID với local handles và cách Direct Binding giữ Global ID ngoài payload. **Bằng chứng:** ví dụ cụ thể bằng ID trong cấu hình và một trace Rx. **Review 2026-09-18:** Chưa đủ — Chưa có bản giải thích từng câu hỏi định danh/Rx kèm trace cụ thể.
- [ ] **A03 — Giải thích COM timing:** vì sao SendSignal không truyền ngay, vì sao I-PDU là đơn vị lập lịch, retry tick sau/không chặn/có giới hạn, drop đúng occurrence nào, vì sao giữ U bit, gộp missed occurrences và không trôi lịch khi CAN_BUSY. **Bằng chứng:** bảng tick với tối thiểu hai I-PDU và câu trả lời đối chiếu T01–T12. **Review 2026-09-18:** Chưa đủ — Chưa có bảng tick hai I-PDU và trả lời đầy đủ câu hỏi scheduling.
- [ ] **A04 — Trình bày được Tx và Rx resolution chain đầy đủ.** **Bằng chứng:** một walkthrough Tx và một walkthrough Rx ghi từng local ID, GlobalPduId, CAN ID, HTH/HRH, Controller, Signal tương ứng. **Review 2026-09-18:** Chưa đủ — Chưa có walkthrough Tx/Rx với toàn bộ ID thực và trace runtime tương ứng.

Ngoài phạm vi bắt buộc Part 1: multiplexed Global PDU/nhúng GlobalPduId lên wire, CanTp và truyền bản tin lớn, deadline monitoring, CanIf Tx buffering và việc sao chép chính xác API AUTOSAR Classic. Chỉ thêm nếu có yêu cầu riêng; không dùng các tính năng này để thay cho Direct CAN Binding hoặc hành vi retry nêu trên. Nguồn: A §2, §17, §37; N §32, §35.
